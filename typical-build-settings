# build-settings
#
# Settings for configuring the type of build to produce.

# DEBUG, RELEASE, or CHECKED
BUILD_TYPE=DEBUG

# unix (gcc), windows (msvc)
PLATFORM=unix

# 32 or 64
POINTER_SIZE=64

# on or off
COVERAGE_TEST=off

# on or off
PROFILE=off

include ../config.${PLATFORM}
